from __future__ import annotations

from pathlib import Path


OUT_DIR = Path(r"C:\Users\yang\Desktop\数据库原理\数据库系统原理\章节知识点与考点总结")


ANSWERS = {
    "01_第1章_数据库系统概论.md": """## 考点答案与范例

1. DB、DBMS、数据库系统、模式、实例、数据独立性的标准回答
   - 数据库 DB：按一定数据模型组织、长期存储、可被多个用户共享的数据集合。例：学校的学生、课程、选课、成绩数据集合。
   - DBMS：数据库管理系统，是位于用户和数据库之间的软件系统，负责定义、存储、查询、更新、并发控制、恢复、安全和完整性控制。
   - 数据库系统 DBS：由数据库、DBMS、应用程序、用户、数据库管理员以及硬件/软件环境组成的整体。
   - 模式 schema：数据库的逻辑结构和约束的描述，较稳定。例如 `student(ID, name, dept_name, tot_cred)`。
   - 实例 instance：某一时刻数据库中的实际数据，随插入、删除、更新而变化。
   - 数据独立性：应用程序不需要因数据组织方式变化而频繁修改。物理数据独立性指存储结构变化不影响逻辑模式和应用；逻辑数据独立性指逻辑模式适度变化不影响外模式和应用。

2. 文件系统与数据库系统比较题答案
   - 文件系统的主要问题是数据冗余和不一致、数据共享困难、数据孤立、访问不灵活、完整性约束难保证、原子性和并发控制不足、安全性和恢复机制弱。
   - 数据库系统通过统一的数据模型、统一管理、共享访问、完整性约束、事务机制、权限控制和恢复机制解决上述问题。
   - 范例答案：文件系统中学生基本信息可能在学籍科、房产科、劳资科多份保存，导致冗余和不一致；数据库系统把公共数据集中管理，各应用通过 DBMS 访问，从而减少冗余并提高一致性。

3. 三级抽象及作用
   - 物理层回答“数据怎样存”：文件、页、记录、索引、磁盘布局。
   - 逻辑层回答“存什么数据以及数据间有什么关系”：表、属性、联系、约束。
   - 视图层回答“不同用户看到什么”：不同用户只看到与自己相关的数据视图。
   - 作用：隐藏复杂性，保护数据，支持数据独立性。考试答题时可写“物理层隐藏存储细节，逻辑层描述整体结构，视图层提供局部表示”。

4. DDL、DML、DCL 的区别
   - DDL 数据定义语言：定义和修改数据库对象，如 `create table`、`alter table`、`drop table`。
   - DML 数据操纵语言：查询和更新数据，如 `select`、`insert`、`delete`、`update`。
   - DCL 数据控制语言：授权和回收权限，如 `grant`、`revoke`。
   - 范例：建学生表用 DDL，查询学生成绩用 DML，允许某用户查询学生表用 DCL。

5. DBMS 组件分工
   - 存储管理器负责磁盘空间、文件组织、缓冲区、索引和记录管理。
   - 查询处理器负责 SQL 解析、关系代数转换、查询优化和执行。
   - 事务管理器负责并发控制和恢复，保证 ACID。
   - 权限和完整性管理模块负责安全控制和约束检查。
   - 范例答案：执行 `select` 时，查询处理器生成执行计划，存储管理器读取页和索引；若执行更新，事务管理器还要保证并发正确性和故障恢复。

6. 基本概念题答题模板
   - 名词解释用“定义 + 作用 + 例子”。
   - 简答比较题用“问题/特点 + 原因 + 后果 + 数据库系统如何解决”。
   - 如问“数据库系统的特点”，可答：数据结构化、共享性高、冗余低、独立性高、由 DBMS 统一管理和控制，支持安全性、完整性、并发控制和恢复。
""",
    "02_第2章_关系模型与关系代数.md": """## 考点答案与范例

1. 关系模型基本概念题答案
   - 关系就是二维表，表名和属性组成关系模式，表中的具体行集合是关系实例。
   - 属性是列，元组是行，域是属性允许取值的集合。
   - 关系中元组无序，重复元组在数学关系模型中不允许；SQL 表可能保留重复，需注意二者差别。
   - 范例：`student(ID, name, dept_name, tot_cred)` 是关系模式；某时刻所有学生记录是该关系的实例。

2. 键的判定答案
   - 超键：能唯一标识元组的属性集。例如学生表中 `{ID}` 是超键，`{ID, name}` 也是超键。
   - 候选键：最小超键，不能再删属性。例如 `{ID}` 是候选键，`{ID, name}` 不是候选键，因为可删去 `name`。
   - 主键：从候选键中选定的一个。
   - 外键：一个关系中的属性引用另一个关系的主键或候选键。例如 `student.dept_name` 引用 `department.dept_name`。

3. 六个基本关系代数运算的准确含义
   - `σ条件(R)`：从 `R` 中选出满足条件的元组，结果属性不变。
   - `π属性列表(R)`：从 `R` 中选出若干属性列，结果自动去重。
   - `R ∪ S`：属于 `R` 或 `S` 的元组，要求两个关系相容。
   - `R - S`：属于 `R` 但不属于 `S` 的元组。
   - `R × S`：`R` 中每个元组与 `S` 中每个元组拼接。
   - `ρ新名(R)` 或 `ρ新名(新属性名)(R)`：给关系或属性重命名，常用于自连接。

4. 连接、自然连接、笛卡尔积的区别
   - 笛卡尔积不加匹配条件，会产生所有组合。
   - 条件连接等价于先笛卡尔积再选择：`R ⋈条件 S = σ条件(R × S)`。
   - 自然连接按同名属性自动等值匹配，并且同名属性只保留一份。
   - 范例：查询选课学生姓名，一般写 `πname(student ⋈ student.ID=takes.ID takes)`；如果两个关系同名属性只有 `ID` 且语义一致，可用自然连接。

5. “所有/全部”问题的范例答案
   - 除法适合表达“满足全部条件”。若 `takes(ID, course_id)`，`course(course_id)`，查询选修了全部课程的学生：
     ```text
     πID(takes) ÷ πcourse_id(course)
     ```
   - 用差集双重否定的思路是：不存在一门课程，使该学生没有选。写关系代数时可先构造“学生应选的所有组合”，再减去实际选课组合，最后从全体学生中减去缺课学生。

6. 外连接和空值题答案
   - 内连接只保留匹配成功的元组。
   - 左外连接保留左关系全部元组，右边无匹配时用 `null` 填充。
   - 右外连接和全外连接同理。
   - 范例：要列出所有课程及其先修课，包括没有先修课的课程，应使用课程表左外连接先修课表。

7. 关系代数表达式答题范例
   - 问：查询工资大于 80000 的教师姓名。
     ```text
     πname(σsalary > 80000(instructor))
     ```
   - 问：查询计算机系教师所教课程号。
     ```text
     πcourse_id(σdept_name='Comp. Sci.'(instructor) ⋈ teaches)
     ```
   - 问：查询有账户但没有贷款的客户。
     ```text
     πcustomer_name(depositor) - πcustomer_name(borrower)
     ```
""",
    "03_第3章_SQL基础.md": """## 考点答案与范例

1. DDL 建表与约束范例
   - 建表题要写清属性、类型、主键、外键和非空约束。
     ```sql
     create table student (
       ID varchar(5),
       name varchar(20) not null,
       dept_name varchar(20),
       tot_cred numeric(3,0),
       primary key (ID),
       foreign key (dept_name) references department(dept_name)
     );
     ```
   - 主键自动要求唯一且非空；外键要求引用值在被引用表中存在，或按具体 DBMS 规则允许为空。

2. 基本查询题答案模板
   - SQL 查询优先按“输出列、数据来源、连接条件、筛选条件、分组条件、分组筛选、排序”组织。
     ```sql
     select name
     from instructor
     where dept_name = 'Comp. Sci.' and salary > 80000;
     ```
   - 多表查询必须写连接条件，否则会产生笛卡尔积。

3. 集合运算准确答案
   - `union` 求并集，默认去重；`union all` 保留重复。
   - `intersect` 求交集；`except` 求差集。
   - 两边查询结果必须列数相同、对应列类型相容。
   - 范例：查询既当教师又当学生的姓名：
     ```sql
     select name from instructor
     intersect
     select name from student;
     ```

4. 空值与三值逻辑答案
   - `null` 表示未知或缺失，不等于 0，也不等于空字符串。
   - 与 `null` 的普通比较结果通常为 `unknown`，不能写 `salary = null`。
   - 正确写法：
     ```sql
     where salary is null
     where salary is not null
     ```
   - `where` 只保留条件为 `true` 的元组，`false` 和 `unknown` 都不会保留。
   - 聚集函数除 `count(*)` 外通常忽略 `null`。

5. 聚集函数、`group by`、`having` 范例
   - 查询每个系的平均工资：
     ```sql
     select dept_name, avg(salary) as avg_salary
     from instructor
     group by dept_name;
     ```
   - 查询平均工资大于 70000 的系：
     ```sql
     select dept_name, avg(salary) as avg_salary
     from instructor
     group by dept_name
     having avg(salary) > 70000;
     ```
   - `where` 在分组前筛选行，`having` 在分组后筛选组。凡是涉及聚集结果的条件，一般放在 `having`。

6. 嵌套子查询与 `not exists` 标准范例
   - `in` 用于判断某值是否属于子查询结果：
     ```sql
     select name
     from student
     where ID in (select ID from takes where course_id = 'CS-101');
     ```
   - “选修所有课程”的经典双重 `not exists`：
     ```sql
     select S.ID, S.name
     from student as S
     where not exists (
       select *
       from course as C
       where not exists (
         select *
         from takes as T
         where T.ID = S.ID and T.course_id = C.course_id
       )
     );
     ```
   - 含义：不存在一门课程 C，使得该学生没有选 C。

7. 数据修改范例
   - 删除某系所有教师：
     ```sql
     delete from instructor
     where dept_name = 'Finance';
     ```
   - 插入一名学生：
     ```sql
     insert into student(ID, name, dept_name, tot_cred)
     values ('12345', 'Zhang', 'Comp. Sci.', 0);
     ```
   - 给工资低于 70000 的教师涨薪 5%：
     ```sql
     update instructor
     set salary = salary * 1.05
     where salary < 70000;
     ```
""",
    "04_第4章_SQL中级.md": """## 考点答案与范例

1. 内连接、自然连接、外连接答案
   - 内连接只保留满足连接条件的匹配元组：
     ```sql
     select course.course_id, title, prereq_id
     from course inner join prereq
       on course.course_id = prereq.course_id;
     ```
   - 自然连接自动按同名属性连接，若存在语义无关的同名属性，可能产生错误结果。
   - 左外连接保留左表所有元组：
     ```sql
     select course.course_id, title, prereq_id
     from course left outer join prereq
       on course.course_id = prereq.course_id;
     ```
   - 若某课程没有先修课，左外连接仍保留该课程，`prereq_id` 为 `null`。

2. 三种连接写法的等价范例
   - 逗号连接：
     ```sql
     select name, title
     from instructor, teaches, course
     where instructor.ID = teaches.ID
       and teaches.course_id = course.course_id;
     ```
   - 显式连接：
     ```sql
     select name, title
     from instructor join teaches on instructor.ID = teaches.ID
       join course on teaches.course_id = course.course_id;
     ```
   - 若同名属性语义一致，可用自然连接，但考试中更推荐写显式 `on` 条件，答案更稳。

3. 视图定义与作用答案
   - 视图是由查询定义的虚关系，本身通常不直接存储数据。
     ```sql
     create view faculty as
     select ID, name, dept_name
     from instructor;
     ```
   - 作用：简化复杂查询、提供逻辑数据独立性、隐藏敏感列、限制用户访问范围。
   - 查询视图时，DBMS 可把视图名展开为其定义查询。

4. 可更新视图判断答案
   - 可更新的关键是：对视图的插入、删除、更新能否唯一翻译成对基表的修改。
   - 通常可更新：基于单个表、没有聚集、没有 `group by`、没有 `distinct`、没有集合运算、没有复杂表达式列，并保留必要键属性的简单视图。
   - 通常不可更新：连接视图、聚集视图、分组视图、去重视图、集合运算视图、只含表达式而无法定位基表列的视图。
   - 范例：`select ID, name from student` 较可能可更新；`select dept_name, avg(salary) from instructor group by dept_name` 不可更新，因为无法把平均工资修改唯一映射到某个教师工资。

5. 完整性约束范例
   - 唯一和非空：
     ```sql
     name varchar(20) not null,
     email varchar(50) unique
     ```
   - 检查约束：
     ```sql
     check (salary > 0)
     ```
   - 外键和级联：
     ```sql
     foreign key (dept_name) references department(dept_name)
       on delete set null
       on update cascade
     ```
   - 答题解释：删除被引用系时，学生或教师中的 `dept_name` 可按策略置空、级联删除或拒绝删除，具体取决于约束定义。

6. 授权语句范例
   - 授权：
     ```sql
     grant select on student to user1;
     grant update(salary) on instructor to user2;
     ```
   - 带转授权：
     ```sql
     grant select on course to user1 with grant option;
     ```
   - 回收权限：
     ```sql
     revoke select on student from user1;
     ```
   - 角色：先给角色授权，再把角色授予用户，便于批量管理。

7. 关系代数与 SQL 转换范例
   - 关系代数：
     ```text
     πname(σdept_name='Comp. Sci.'(instructor))
     ```
   - SQL：
     ```sql
     select name
     from instructor
     where dept_name = 'Comp. Sci.';
     ```
   - 转换要点：投影对应 `select`，选择对应 `where`，连接对应 `from/join/on`。
""",
    "05_第5章_SQL高级.md": """## 考点答案与范例

1. JDBC、ODBC、嵌入式 SQL 的区别
   - JDBC 是 Java 访问数据库的标准 API，程序通过连接对象、语句对象和结果集对象执行 SQL。
   - ODBC 是开放数据库连接标准，强调跨语言、跨数据库，通过驱动管理器和数据库驱动访问数据库。
   - 嵌入式 SQL 是把 SQL 语句写入宿主语言程序中，预编译后执行，常用宿主变量和游标传递数据。
   - 范例答案：Java 程序通常用 JDBC；C/C++ 跨数据库访问可用 ODBC；需要在宿主语言中直接嵌入 SQL 时用嵌入式 SQL。

2. 游标的作用与标准回答
   - SQL 查询结果可能有多行，而宿主语言一次通常处理一条记录。
   - 游标用于定位查询结果集中的当前行，并通过 `fetch` 逐行取出。
   - 标准流程：声明游标、打开游标、循环取记录、关闭游标。
   - 范例解释：查询所有选课超过某学分的学生时，结果可能有多名学生，程序需要游标逐条读取 `ID` 和 `name`。

3. 函数与存储过程的区别
   - 函数通常有返回值，可在 SQL 表达式中使用，强调计算一个值。
   - 存储过程是一组数据库操作的封装，可有输入、输出参数，强调完成业务流程。
   - 存储过程可减少网络通信、复用逻辑、集中权限控制，但可移植性较差。
   - 范例：计算教师年薪可写函数；完成选课、扣容量、记录日志的一组操作更适合写存储过程。

4. 存储过程范例
   ```sql
   create procedure raise_salary(in dept varchar(20), in rate numeric(4,2))
   begin
     update instructor
     set salary = salary * (1 + rate)
     where dept_name = dept;
   end;
   ```
   - 答题说明：该过程接收系名和涨薪比例，批量更新指定系教师工资。

5. 触发器标准答案
   - 触发器由触发事件、触发条件和触发动作组成。
   - 触发事件包括 `insert`、`delete`、`update`。
   - 触发粒度包括行级触发和语句级触发。
   - 用途：维护派生数据、审计日志、复杂完整性约束。
   - 范例：向 `takes` 插入及格成绩后，自动更新学生已获学分。

6. 触发器使用边界
   - 适合：必须在数据库层自动维护的一致性规则，如日志、派生字段、跨表约束。
   - 不适合：本可由外键、`check`、普通 SQL 明确表达的规则；复杂业务流程全部隐藏在触发器中；可能产生递归触发的操作。
   - 答题关键词：隐藏副作用、调试困难、可移植性差、可能影响性能。

7. 递归查询范例
   - 递归查询用于传递闭包，例如求所有直接或间接先修课。
   - 思路：先给出基础查询，再反复根据已有结果扩展，直到没有新结果。
   - 答题中即使不要求完整 SQL，也应说明“基础部分 + 递归部分 + 固定点停止”。
""",
    "06_第6章_ER模型.md": """## 考点答案与范例

1. ER 图设计题标准步骤
   - 第一步：从需求名词中找实体集，如学生、课程、教师、院系。
   - 第二步：为实体集列属性，并标出主键。
   - 第三步：从动词或业务规则中找联系集，如选修、讲授、属于、指导。
   - 第四步：标出映射基数，一对一、一对多、多对多。
   - 第五步：标出参与约束，全部参与用双线，部分参与用单线。
   - 第六步：检查弱实体、多值属性、联系属性、递归联系和聚集是否遗漏。

2. 实体集、属性集、联系集答案
   - 实体集是同类对象集合，如 `student`。
   - 属性集描述实体或联系的性质，如学生的 `ID`、`name`。
   - 联系集描述实体之间的关联，如学生和课程之间的 `takes`。
   - 描述性属性属于联系本身。例如学生选课联系中的 `grade`，不是单独属于学生或课程。

3. 弱实体集答案
   - 弱实体集没有足够属性独立形成主键，必须依赖强实体集存在。
   - 弱实体集有分辨符，但分辨符只在同一拥有者实体内唯一。
   - 弱实体的主键 = 拥有者实体主键 + 弱实体分辨符。
   - 范例：课程的开课班次 `section` 可依赖 `course`，若 `sec_id` 只在某课程内唯一，则 `section` 的键可为 `(course_id, sec_id, semester, year)`。

4. 映射基数与参与约束范例
   - 一对一：一个实体最多对应另一个实体的一个实体，如一个院系最多有一名主任。
   - 一对多：一个院系有多名教师，每名教师属于一个院系。
   - 多对多：学生可选多门课，一门课可被多名学生选。
   - 全部参与：每个实体都必须参加联系，如每个弱实体必须属于某个强实体。
   - 部分参与：实体可以不参加联系，如教师可以暂时不讲授课程。

5. ER 到关系模式转换答案
   - 强实体集：直接转换为关系，主键保持不变。
   - 复合属性：拆成简单属性；派生属性一般不存储。
   - 多值属性：单独建关系，属性包括实体主键和多值属性。
   - 弱实体集：建关系，加入拥有者主键，主键为拥有者主键加分辨符。
   - 1:n 联系：把 1 端主键作为外键放入 n 端，并加入联系属性。
   - m:n 联系：单独建关系，包含两端主键和联系属性。
   - 1:1 联系：可任选一端加入另一端主键作为外键，优先放在全部参与的一端。

6. 转换范例
   - 学生 `student(ID, name)`，课程 `course(course_id, title)`，选修联系 `takes` 有属性 `grade`，学生和课程是多对多。
   - 转换结果：
     ```text
     student(ID, name)
     course(course_id, title)
     takes(ID, course_id, grade)
     ```
   - `takes` 的主键通常为 `(ID, course_id)`，同时 `ID` 和 `course_id` 分别是外键。

7. 聚集、特化、泛化答案
   - 聚集：当一个联系还要参与另一个联系时，把该联系连同相关实体看成一个高层抽象对象。
   - 特化：从高层实体集划分出低层子类，如 `person` 特化为 `student` 和 `instructor`。
   - 泛化：把多个低层实体集抽象成共同高层实体集。
   - 约束包括互斥/重叠、全部/部分。

8. 常见扣分点
   - 漏主键或外键。
   - 漏联系属性，如选课成绩。
   - 把多对多联系错误地放到某一端实体表中。
   - 把多值属性直接塞进原实体表，违反 1NF。
   - 三元联系被随意拆成多个二元联系，导致语义丢失。
""",
    "07_第7章_关系数据库设计与规范化.md": """## 考点答案与范例

1. 函数依赖判断答案
   - `X -> Y` 的含义是：在任意合法关系实例中，若两个元组的 `X` 属性值相同，则它们的 `Y` 属性值也必须相同。
   - 判断时不能只看当前表中碰巧成立的数据，还要看业务语义是否必然成立。
   - 范例：若学号唯一确定姓名，则 `ID -> name` 成立；姓名通常不能唯一确定学号，所以 `name -> ID` 不一定成立。

2. 属性闭包计算范例
   - 已知 `R(A,B,C,D,E)`，`F={A->BC, CD->E, B->D, E->A}`，求 `A+`。
   - 初始：`A+ = {A}`。
   - 用 `A->BC`，得 `{A,B,C}`。
   - 用 `B->D`，得 `{A,B,C,D}`。
   - 用 `CD->E`，因已有 C、D，得 `{A,B,C,D,E}`。
   - 所以 `A+ = ABCDE`，`A` 是超键。若没有真子集也是超键，则 `A` 是候选键。

3. 候选键计算答案
   - 先找不出现在任何依赖右部的属性，这些属性通常必须包含在候选键中。
   - 再通过属性闭包试探能否推出全部属性。
   - 最后检查最小性，删除任一属性后不能再推出全部属性。
   - 答题必须写闭包过程，不能只写结果。

4. Armstrong 公理和推理范例
   - 自反律：若 `Y ⊆ X`，则 `X -> Y`。
   - 增广律：若 `X -> Y`，则 `XZ -> YZ`。
   - 传递律：若 `X -> Y` 且 `Y -> Z`，则 `X -> Z`。
   - 常用推论：合并律、分解律、伪传递律。
   - 范例：由 `A->B` 和 `B->C`，用传递律可得 `A->C`；由 `A->B` 和 `A->C`，用合并律可得 `A->BC`。

5. 正则覆盖答案
   - 目标是把依赖集化简为等价、右部单属性、无多余属性、无多余依赖的形式。
   - 步骤：右部分解为单属性；检查左部是否有多余属性；检查右部或整条依赖是否多余；可合并同左部依赖。
   - 检查多余属性时，核心方法是比较删除前后的闭包是否仍能推出相同依赖。
   - 3NF 分解通常先求正则覆盖，再按依赖建关系。

6. 无损连接证明范例
   - 对二元分解 `R` 分为 `R1`、`R2`，若 `(R1 ∩ R2) -> R1` 或 `(R1 ∩ R2) -> R2` 属于 `F+`，则分解无损。
   - 范例：`R(A,B,C)` 分解为 `R1(A,B)` 和 `R2(B,C)`，若 `B -> A` 或 `B -> C` 成立，则无损。
   - 如果公共属性不能决定任一子模式，则自然连接可能产生伪元组，是有损分解。

7. 依赖保持判断答案
   - 依赖保持要求原依赖集中的约束可通过各子关系上的依赖检查完成，不需要把子关系连接起来。
   - 方法：把原依赖投影到各子模式，求这些投影依赖的闭包，看能否推出原依赖集。
   - 结论：3NF 分解算法可保证无损且保持依赖；BCNF 分解保证无损，但不一定保持依赖。

8. BCNF 判定与分解范例
   - BCNF 条件：每个非平凡函数依赖 `X -> Y` 的左部 `X` 都必须是超键。
   - 若发现 `X -> Y` 违反 BCNF，则分解为：
     ```text
     R1 = X ∪ Y
     R2 = R - (Y - X)
     ```
   - 范例：`R(A,B,C)`，`F={A->B, B->C}`。`A+ = ABC`，A 是键；`B+ = BC`，B 不是超键，所以 `B->C` 违反 BCNF。分解为 `R1(B,C)` 和 `R2(A,B)`。

9. 3NF 判定与分解范例
   - 3NF 条件：对每个非平凡依赖 `X -> A`，至少满足 `X` 是超键，或 `A` 是主属性。
   - 主属性是出现在某个候选键中的属性。
   - 3NF 分解步骤：求正则覆盖；对每个 `X->A` 建关系 `XA`；若没有任何关系包含候选键，则补一个候选键关系；删除被其他关系包含的关系。
   - 3NF 相比 BCNF 的优势是能保持依赖，代价是可能保留少量冗余。

10. 多值依赖答案
   - `X ->-> Y` 表示在给定 `X` 时，`Y` 的取值集合与其余属性取值集合相互独立。
   - 范例：某课程有多个教师、多个教材，教师和教材彼此独立，则可能有 `course ->-> teacher` 和 `course ->-> book`。
   - 多值依赖会造成组合冗余，是 4NF 讨论的基础。本课程一般要求理解概念，不一定要求复杂分解。
""",
    "17_第17章_事务.md": """## 考点答案与范例

1. 事务和 ACID 标准答案
   - 事务是数据库操作的逻辑工作单位，要么完整执行，要么不执行。
   - 原子性：转账中扣 A 和加 B 必须作为整体完成，不能只扣不加。
   - 一致性：转账前后 A+B 总额不变。
   - 隔离性：并发事务不能看到转账中间状态。
   - 持久性：转账提交后，即使系统崩溃，结果也不能丢失。

2. 事务状态题答案
   - Active：事务正在执行读写操作。
   - Partially committed：最后一条语句执行完，但提交尚未最终完成。
   - Committed：提交成功，结果持久化。
   - Failed：发现错误，不能继续正常执行。
   - Aborted：事务回滚，数据库恢复到事务开始前状态。
   - 常见状态转换：Active -> Partially committed -> Committed；Active/Partially committed -> Failed -> Aborted。

3. 冲突操作判定答案
   - 两个操作冲突必须同时满足：不同事务、同一数据项、至少一个写。
   - `read_i(X)` 与 `read_j(X)` 不冲突。
   - `read_i(X)` 与 `write_j(X)` 冲突。
   - `write_i(X)` 与 `read_j(X)` 冲突。
   - `write_i(X)` 与 `write_j(X)` 冲突。

4. 冲突可串行化判定范例
   - 调度：
     ```text
     r1(A), w1(A), r2(A), w2(A), r1(B), w1(B), r2(B), w2(B)
     ```
   - A 上 `w1(A)` 在 `r2(A)、w2(A)` 前，得边 `T1 -> T2`。
   - B 上 `w1(B)` 也在 `r2(B)、w2(B)` 前，仍是 `T1 -> T2`。
   - 优先图无环，所以冲突可串行化，等价串行顺序为 `T1, T2`。

5. 优先图有环范例
   - 若 A 上产生 `T1 -> T2`，B 上又产生 `T2 -> T1`，优先图有环。
   - 有环说明不能通过交换非冲突操作变成串行调度，所以不是冲突可串行化。
   - 答题时必须写出边的来源，不能只画结论。

6. 可恢复调度答案
   - 若 `Tj` 读了 `Ti` 写的数据，则 `Tj` 必须在 `Ti` 提交之后才能提交。
   - 若 `Tj` 在 `Ti` 提交前提交，而之后 `Ti` 回滚，则 `Tj` 已提交了基于脏数据的结果，调度不可恢复。
   - 判断方法：找所有“读了谁写的数据”的关系，再检查提交顺序。

7. 无级联调度答案
   - 无级联要求事务只能读取已提交事务写入的数据。
   - 如果 `Tj` 读取了 `Ti` 未提交写入的数据，则可能因 `Ti` 回滚导致 `Tj` 也回滚，形成级联回滚。
   - 无级联调度一定是可恢复调度，但可恢复调度不一定无级联。

8. 隔离级别和异常答案
   - Read Uncommitted：可能脏读、不可重复读、幻读。
   - Read Committed：避免脏读，但可能不可重复读、幻读。
   - Repeatable Read：避免脏读和不可重复读，但标准意义下可能幻读。
   - Serializable：效果等价于串行执行，避免上述异常。
   - 丢失修改：两个事务同时更新同一数据，后写覆盖前写。
   - 脏读：读到未提交数据。
   - 不可重复读：同一事务两次读同一行结果不同。
   - 幻读：同一事务两次按条件查询，出现新增或消失的满足条件行。

9. 视图可串行化答案
   - 视图等价要求读从关系一致、最终写一致。
   - 视图可串行化比冲突可串行化更宽松，所有冲突可串行化调度都是视图可串行化，但反过来不一定。
   - 含盲写的调度可能视图可串行化但非冲突可串行化。
""",
    "18_第18章_并发控制.md": """## 考点答案与范例

1. S 锁和 X 锁相容性答案
   - S 锁用于读，多个事务可同时对同一数据项持有 S 锁。
   - X 锁用于写，与其他任何事务在同一数据项上的 S 锁或 X 锁都不相容。
   - 相容矩阵：
     ```text
           S   X
     S     是  否
     X     否  否
     ```
   - 判断题只看“同一数据项”和“不同事务”的锁是否相容。

2. 两阶段锁 2PL 判定答案
   - 2PL 要求每个事务有两个阶段：增长阶段只加锁不解锁，收缩阶段只解锁不加锁。
   - 判断方法：对每个事务单独检查，找到第一次 `unlock`，看其后是否还有新的 `lock`。如果有，则违反 2PL。
   - 2PL 保证冲突可串行化，但可能发生死锁。

3. 2PL 判定范例
   - 满足 2PL：
     ```text
     T1: lock-X(A), lock-S(B), read(B), write(A), unlock(A), unlock(B)
     ```
     第一次解锁后没有再申请锁。
   - 不满足 2PL：
     ```text
     T1: lock-S(A), read(A), unlock(A), lock-S(B), read(B), unlock(B)
     ```
     释放 A 后又申请 B，违反两阶段规则。

4. 严格 2PL 与强 2PL 区别
   - 普通 2PL：释放锁后不能再申请新锁，但可在提交前释放某些锁。
   - 严格 2PL：所有 X 锁必须持有到事务提交或回滚，可避免其他事务读到未提交写入，防止级联回滚。
   - 强 2PL：所有锁，包括 S 锁和 X 锁，都持有到提交或回滚，实现最简单但并发度更低。
   - 考试区分关键词：严格 2PL 看 X 锁，强 2PL 看所有锁。

5. 死锁检测答案
   - 等待图中顶点是事务。
   - 若 `Ti` 等待 `Tj` 持有的锁，则画边 `Ti -> Tj`。
   - 等待图有环则存在死锁。
   - 范例：`T1` 持有 A 等 B，`T2` 持有 B 等 A，则有 `T1 -> T2` 和 `T2 -> T1`，形成死锁。

6. 死锁恢复答案
   - 检测到死锁后，选择一个或多个牺牲事务回滚，释放其锁，使其他事务继续。
   - 选择牺牲事务可考虑已执行代价、已持有锁数量、回滚代价、事务优先级、是否避免饥饿。
   - 回滚可完全回滚，也可回滚到检查点。
   - 为避免同一事务总被牺牲，需要考虑饥饿问题。

7. 死锁预防答案
   - Wait-die：非抢占。老事务请求新事务持有的锁时等待；新事务请求老事务持有的锁时回滚。
   - Wound-wait：抢占。老事务请求新事务持有的锁时迫使新事务回滚；新事务请求老事务持有的锁时等待。
   - 两者都用时间戳保证等待方向有序，从而避免等待环。

8. 多粒度锁与意向锁答案
   - 多粒度锁允许在数据库、表、页、记录等不同层次加锁。
   - 意向锁表示事务打算在更低层加 S 锁或 X 锁。
   - IS：意向共享锁，表示低层将加 S 锁。
   - IX：意向排他锁，表示低层将加 X 锁。
   - SIX：当前层加 S 锁，同时低层可能加 X 锁。
   - 加锁顺序通常自顶向下，解锁顺序自底向上。

9. 时间戳、验证、多版本答案
   - 时间戳协议：按事务时间戳顺序安排读写，若操作违反时间戳顺序则回滚或拒绝。
   - Thomas 写规则：若某写操作已经过时且不会被后续读到，可忽略该写，从而提高并发度。
   - 验证协议：事务先执行读和本地写，提交前验证是否冲突，适合冲突少的场景。
   - 多版本机制：保存数据多个版本，读事务读取合适版本，减少读写冲突。
   - 快照隔离：事务读取开始时或某一时刻的一致快照，写冲突通常采用先提交者获胜。

10. 并发控制综合答题模板
   - 先判断使用哪类协议：锁、时间戳、验证还是多版本。
   - 锁题先写相容性，再检查 2PL，再看是否严格或强。
   - 死锁题先画等待图，再看环，再写恢复策略。
   - 隔离级别题把异常和第 17 章对应起来：脏读、不可重复读、幻读、丢失修改。
""",
}


def insert_section(path: Path, section: str) -> bool:
    text = path.read_text(encoding="utf-8")
    if "## 考点答案与范例" in text:
        return False
    marker = "\n## 常见题型与答题抓手"
    if marker not in text:
        raise ValueError(f"missing marker in {path}")
    updated = text.replace(marker, "\n" + section.strip() + "\n" + marker, 1)
    path.write_text(updated, encoding="utf-8")
    return True


def main() -> None:
    changed = []
    skipped = []
    for filename, section in ANSWERS.items():
        path = OUT_DIR / filename
        if insert_section(path, section):
            changed.append(filename)
        else:
            skipped.append(filename)
    print("changed:", len(changed), ", ".join(changed))
    print("skipped:", len(skipped), ", ".join(skipped))


if __name__ == "__main__":
    main()
