from __future__ import annotations

from pathlib import Path


OUT_DIR = Path(r"C:\Users\yang\Desktop\数据库原理\数据库系统原理\章节知识点与考点总结")


SUMMARIES = {
    "01_第1章_数据库系统概论.md": """# 第1章 数据库系统概论

## 章节定位

本章是全课程的概念入口，重点理解数据库系统为什么产生、DBMS 的作用、数据抽象层次、数据模型、数据库语言、数据库体系结构，以及存储、查询、事务等系统组件的基本分工。

## 核心知识点

1. 数据库系统基本概念
   - 数据库：长期存储在计算机内、有组织、可共享的数据集合。
   - DBMS：管理数据库的软件，提供数据定义、查询更新、事务管理、安全控制、并发控制、恢复等功能。
   - 数据库系统：数据库、DBMS、应用程序、用户、数据库管理员和运行环境的整体。
   - 数据独立性：应用程序与数据存储细节解耦，分为物理数据独立性和逻辑数据独立性。

2. 文件系统的局限
   - 数据冗余和不一致。
   - 数据访问困难，新增需求常需重新编程。
   - 数据孤立，格式不统一。
   - 完整性约束难维护。
   - 原子性、并发访问、安全控制和故障恢复能力不足。

3. 数据管理的发展阶段
   - 人工管理阶段：数据不保存或保存能力弱，程序与数据高度耦合。
   - 文件系统阶段：文件可长期保存，但共享性差、冗余高。
   - 数据库系统阶段：统一管理、共享性高、冗余低、独立性强。

4. 数据抽象与模式
   - 物理层：描述数据实际如何存储。
   - 逻辑层：描述存储哪些数据以及数据间关系。
   - 视图层：面向不同用户的局部数据表示。
   - 实例：某一时刻数据库中的实际数据。
   - 模式：数据库的总体逻辑结构。

5. 数据模型
   - 层次模型、网状模型、关系模型、对象关系模型、XML、NoSQL 等。
   - 关系模型用表表示数据和联系，是后续关系代数、SQL、规范化的基础。
   - NoSQL 常见类型包括键值、文档、列族、图数据库，强调扩展性、灵活模式和高可用。

6. 数据库语言
   - DDL：定义数据库模式和约束，如 `create table`。
   - DML：查询和更新数据，如 `select`、`insert`、`delete`、`update`。
   - SQL：集 DDL、DML、DCL 等于一体的关系数据库标准语言。

7. 数据库系统组件
   - 存储管理器：管理文件、索引、缓冲区、页面、记录。
   - 查询处理器：解析、优化、执行查询。
   - 事务管理器：保证 ACID、并发控制和故障恢复。
   - DBA：负责模式定义、权限、安全、备份恢复、性能调优。

8. 数据库体系结构与应用
   - 集中式、客户机/服务器、分布式数据库、云托管数据库、云原生数据库。
   - OLTP 偏事务处理，OLAP 偏分析查询，HTAP 兼顾事务与分析。

## 考点整理

- 会解释 DB、DBMS、数据库系统、模式、实例、数据独立性等基本概念。
- 会比较文件系统与数据库系统，重点答冗余、不一致、共享性、独立性、完整性、并发、恢复、安全。
- 会说明三级数据抽象及其作用。
- 会区分 DDL、DML、DCL 的职责。
- 会从组件角度说明 DBMS 如何完成存储、查询和事务管理。
- 复习课件明确提示：第 1 章主要考基本概念。

## 常见题型与答题抓手

- 名词解释：用“定义 + 作用 + 例子”回答。
- 简答：文件系统缺点、数据库系统特点、数据独立性的意义。
- 判断/选择：模式与实例、逻辑层与物理层、DDL 与 DML 的区别。
""",
    "02_第2章_关系模型与关系代数.md": """# 第2章 关系模型与关系代数

## 章节定位

本章是关系数据库的形式化基础。重点掌握关系、属性、元组、域、键、关系模式，以及关系代数的基本运算、附加运算、扩展运算，能根据自然语言需求写出关系代数表达式。

## 核心知识点

1. 关系模型基本概念
   - 关系：二维表。
   - 属性：表的列。
   - 元组：表的行。
   - 域：属性允许取值的集合。
   - 关系模式：`R(A1, A2, ..., An)`。
   - 关系实例：某一时刻关系中的元组集合。
   - 关系是无序的：元组顺序、属性显示顺序一般不影响关系含义。
   - `null` 表示未知、不适用或缺失值。

2. 键
   - 超键：能唯一标识元组的属性集。
   - 候选键：最小超键。
   - 主键：被选为主要标识的候选键。
   - 外键：引用另一个关系主键或候选键的属性集，用于表达参照完整性。

3. 关系代数基本运算
   - 选择 `σ`：选行。
   - 投影 `π`：选列，并自动去重。
   - 并 `∪`：两个相容关系的元组合并。
   - 差 `-`：属于第一个关系但不属于第二个关系的元组。
   - 笛卡尔积 `×`：两个关系所有元组组合。
   - 重命名 `ρ`：改关系名或属性名，常用于自连接和消除重名。

4. 附加关系代数运算
   - 交 `∩`：可由差运算表达。
   - 连接 `⋈`：选择加笛卡尔积的组合。
   - 自然连接：按同名属性自动等值连接，并保留一份同名属性。
   - 除法 `÷`：用于“查询满足全部条件”的问题，例如选修全部课程的学生。
   - 赋值：把中间结果命名，便于表达复杂查询。

5. 扩展关系代数
   - 广义投影：投影中可含表达式。
   - 聚集：`avg`、`min`、`max`、`sum`、`count`，可结合分组。
   - 外连接：左外连接、右外连接、全外连接，用 `null` 保留不匹配信息。

6. 数据库修改
   - 删除：从关系中去除满足条件的元组。
   - 插入：加入新元组或查询结果。
   - 更新：用表达式替换属性值。

## 考点整理

- 复习课件明确提示：第 2 章重点是关系代数表达式，包括基本运算、附加运算、扩展运算。
- 会判断关系模式、关系实例、域、键。
- 会写选择、投影、连接、自然连接、并、差、交、除法表达式。
- 会处理“至少”“所有”“不存在”“只选修了”等自然语言条件。
- 会区分笛卡尔积、等值连接、自然连接。
- 会使用重命名解决自连接问题。
- 会理解外连接与 `null` 的引入。

## 常见题型与答题抓手

- “查询满足某条件的元组”：先选择 `σ`，再投影 `π`。
- “同时满足两个集合条件”：常用交、连接或嵌套选择。
- “不满足某条件”：常用差。
- “全部/所有”：优先考虑除法；也可用差集双重否定表达。
- “至少有一个”：用连接或存在式思路。
- “没有任何”：用差或 `not exists` 对应的关系代数表达。
""",
    "03_第3章_SQL基础.md": """# 第3章 SQL 基础

## 章节定位

本章从 SQL 的数据定义和基本查询开始，逐步覆盖集合运算、空值、聚集、分组、嵌套子查询以及数据库修改，是考试中最容易出编程题和查询题的部分。

## 核心知识点

1. DDL 与表定义
   - `create table` 定义关系名、属性名、数据类型和完整性约束。
   - 常见类型：`char(n)`、`varchar(n)`、`int`、`smallint`、`numeric(p,d)`、`real`、`double precision`、日期时间类型等。
   - 常见约束：`not null`、`primary key`、`foreign key ... references ...`。
   - `drop table` 删除表，`alter table` 修改表结构。

2. 基本查询结构
   - 基本形式：`select ... from ... where ...`。
   - `select` 指定输出属性，可使用表达式和别名。
   - `from` 指定查询来源，多个表默认形成笛卡尔积。
   - `where` 指定筛选条件。
   - SQL 默认保留重复行，去重需用 `distinct`。
   - 排序用 `order by`，可指定升序/降序。

3. 字符串与谓词
   - `like` 进行模式匹配。
   - `%` 匹配任意长度字符串，`_` 匹配单个字符。
   - `between`、比较运算符、逻辑运算 `and/or/not`。

4. 集合运算
   - `union`、`intersect`、`except` 默认去重。
   - `union all`、`intersect all`、`except all` 保留重复。
   - 参与集合运算的查询结果需列数和类型相容。

5. 空值与三值逻辑
   - `null` 参与比较的结果通常是 `unknown`。
   - SQL 条件判断存在 `true`、`false`、`unknown`。
   - 判断空值必须用 `is null` 或 `is not null`，不能用 `= null`。
   - 聚集函数通常忽略 `null`，但 `count(*)` 统计元组数。

6. 聚集与分组
   - 聚集函数：`avg`、`min`、`max`、`sum`、`count`。
   - `group by` 按属性分组。
   - `having` 筛选分组，`where` 筛选分组前的元组。
   - `select` 中非聚集属性通常必须出现在 `group by` 中。

7. 嵌套子查询
   - `in` / `not in`：集合成员测试。
   - `some` / `all`：集合比较。
   - `exists` / `not exists`：判断子查询结果是否为空。
   - `not exists` 常用于表达“所有”或“不存在反例”。
   - 子查询可出现在 `where`、`from`、`select` 中。
   - `with` 子句可定义临时关系，增强复杂查询可读性。

8. 数据库修改
   - `delete from ... where ...` 删除元组。
   - `insert into ... values ...` 或插入查询结果。
   - `update ... set ... where ...` 更新属性值。
   - `case when ... then ... else ... end` 可实现条件更新。

## 考点整理

- 复习课件提示：第 3、4、5 章合并考 SQL，覆盖 DDL、DML、DCL、集合运算、空值、聚集函数、`having`、`not exists`、嵌套子查询、视图、可更新视图、完整性约束、函数、过程、触发器。
- 第 3 章尤其要会写查询语句，能把自然语言问题转换为 SQL。
- 会区分 `where` 与 `having`。
- 会处理 `null` 和三值逻辑。
- 会用 `not exists` 表达“全部”。
- 会写增删改语句，注意子查询更新的含义。

## 常见题型与答题抓手

- “每个系/每门课/每个学生的数量或平均值”：`group by` + 聚集函数。
- “分组后满足条件”：`having`。
- “选修了所有课程”：双重 `not exists`。
- “没有选某课/没有记录”：`not exists` 或 `not in`，但含 `null` 时优先 `not exists`。
- “排名/排序输出”：`order by`。
""",
    "04_第4章_SQL中级.md": """# 第4章 SQL 中级

## 章节定位

本章继续扩展 SQL，重点是连接表达式、视图、事务语句、完整性约束、SQL 类型与模式、索引、授权，以及关系代数与 SQL 的转换。

## 核心知识点

1. 连接表达式
   - `inner join ... on ...`：按连接条件匹配。
   - `natural join`：按同名属性自动连接。
   - `left outer join`：保留左表不匹配元组。
   - `right outer join`：保留右表不匹配元组。
   - `full outer join`：保留两侧不匹配元组。
   - 外连接用 `null` 填充缺失一侧的属性。

2. 连接写法
   - 逗号连接加 `where`。
   - 显式 `join ... on ...`。
   - 自然连接 `natural join`。
   - 考试中应明确连接条件，避免自然连接误按无关同名属性连接。

3. 视图
   - `create view v as <query>` 定义虚关系。
   - 视图可隐藏复杂查询、提供逻辑独立性、限制用户访问范围。
   - 视图展开：查询视图时可替换为其定义查询。
   - 物化视图存储查询结果，需要维护刷新。

4. 视图更新
   - 简单单表视图较可能可更新。
   - 含聚集、分组、去重、集合运算、连接、表达式列的视图通常不可直接更新或更新含义不唯一。
   - 可更新视图的核心问题是：视图上的修改能否唯一翻译成基表修改。

5. 事务
   - SQL 中事务由一组语句组成。
   - `commit work` 提交，`rollback work` 回滚。
   - 更完整的事务性质在第 17 章展开。

6. 完整性约束
   - 单关系约束：`not null`、`unique`、`check`。
   - 参照完整性：`foreign key ... references ...`。
   - 级联动作：`on delete cascade`、`on update cascade`，以及置空、拒绝等策略。
   - 复杂约束可用断言或触发器表达。

7. SQL 数据类型、模式与索引
   - 内置类型、用户自定义类型、域、LOB。
   - `create index` 用于建立索引，加速查询但会增加更新维护成本。
   - B+ 树索引是数据库常见索引结构。

8. 授权
   - 权限包括 `select`、`insert`、`update`、`delete`、`references`、`usage` 等。
   - `grant` 授权，`revoke` 回收权限。
   - `with grant option` 允许被授权者继续授权。
   - 角色 `role` 便于统一管理权限。
   - 视图可用于行列级访问控制。

## 考点整理

- 会写内连接、外连接，并说明外连接为什么引入 `null`。
- 会判断视图是否可更新，说明不能更新的原因。
- 会写完整性约束，尤其主键、外键、`check`、级联。
- 会写授权和回收权限语句。
- 会进行关系代数与 SQL 的相互转换。

## 常见题型与答题抓手

- “保留未匹配记录”：使用外连接。
- “只允许用户看部分列/行”：定义视图并授权视图。
- “删除父表记录对子表影响”：看外键的级联/置空/拒绝策略。
- “视图更新是否合法”：检查是否存在唯一对应的基表元组和属性。
""",
    "05_第5章_SQL高级.md": """# 第5章 SQL 高级

## 章节定位

本章关注 SQL 与程序语言的结合，以及数据库端程序能力，包括 JDBC、ODBC、嵌入式 SQL、函数、存储过程、触发器和递归查询。

## 核心知识点

1. 应用程序访问数据库
   - 应用通过 API 连接数据库服务器、发送 SQL、获取结果、提交或回滚事务。
   - 典型流程：建立连接、创建语句、执行查询或更新、处理结果集、关闭资源。

2. JDBC
   - Java 访问数据库的标准 API。
   - 支持查询、更新、结果集读取、元数据获取。
   - 常见对象：连接、语句、预编译语句、结果集。

3. ODBC
   - 开放数据库连接标准。
   - 提供跨语言、跨数据库的访问接口。
   - 应用通过 ODBC 驱动与数据库通信。

4. 嵌入式 SQL
   - 把 SQL 嵌入宿主语言。
   - 使用宿主变量在程序与 SQL 间传递值。
   - 游标用于逐行处理多元组查询结果。
   - 查询前需声明游标，执行后打开游标，逐条 `fetch`，最后关闭。

5. 函数与存储过程
   - 函数通常有返回值，可用于表达式。
   - 存储过程更强调完成一组操作，可有输入/输出参数。
   - 优点：封装业务逻辑、减少网络交互、便于权限控制和复用。
   - 缺点：可移植性较差，过度使用会增加数据库端负担。

6. 触发器
   - 由事件、条件、动作组成。
   - 触发事件可为 `insert`、`delete`、`update`。
   - 可在语句级或行级触发。
   - 可用于维护派生数据、审计日志、复杂完整性约束。
   - 不宜滥用触发器替代显式逻辑，避免递归触发、调试困难和隐藏副作用。

7. 递归查询
   - SQL 递归常用 `with recursive` 表达。
   - 适合处理传递闭包、层次结构、路径可达性等问题。
   - 固定点计算：不断加入新结果，直到结果不再变化。

## 考点整理

- 复习课件提示 SQL 考点包含函数、过程、触发器。
- 会说明 JDBC/ODBC/嵌入式 SQL 的作用和差别。
- 会理解游标为什么用于多行结果。
- 会写或读懂简单函数、存储过程、触发器。
- 会判断触发器的触发时机、粒度和用途。
- 了解递归查询的基本思想。

## 常见题型与答题抓手

- “程序如何读取多行查询结果”：答游标或结果集。
- “自动维护统计字段”：可用触发器，但要说明触发事件和动作。
- “封装一组数据库操作”：使用存储过程。
- “求先修课传递关系/组织上下级”：使用递归查询。
""",
    "06_第6章_ER模型.md": """# 第6章 ER 模型

## 章节定位

本章是数据库概念设计的核心，要求能根据业务需求画完整 ER 图，并能把 ER 模型转换为关系模式。复习课件明确把 ER 模型设计、关系模型转换和 ER 图完整性列为重点。

## 核心知识点

1. 数据库设计阶段
   - 需求分析：弄清用户数据需求和业务规则。
   - 概念设计：用 ER 模型描述实体、属性、联系和约束。
   - 逻辑设计：转换为关系模式。
   - 物理设计：选择存储结构、索引等实现细节。

2. 实体集与属性
   - 实体：现实世界中可区分的对象。
   - 实体集：同类实体的集合。
   - 属性：实体或联系的性质。
   - 简单属性与复合属性。
   - 单值属性与多值属性。
   - 派生属性与冗余属性。

3. 联系集
   - 联系：实体之间的关联。
   - 联系集可以有描述性属性。
   - 联系度：二元、三元和更高元联系。
   - 递归联系：同一实体集内部实体之间的联系。
   - 角色名用于区分递归联系或同一实体集多次参与联系的不同身份。

4. 约束
   - 映射基数：一对一、一对多、多对一、多对多。
   - 参与约束：全部参与与部分参与。
   - 主键：实体集和联系集都要能唯一标识。
   - 弱实体集：没有足够属性形成主键，依赖强实体集存在。
   - 标识联系：弱实体集与其拥有者实体集之间的联系。

5. ER 图符号
   - 矩形表示实体集。
   - 菱形表示联系集。
   - 椭圆表示属性。
   - 双矩形/双菱形表示弱实体集及标识联系。
   - 线、箭头、双线表示基数和参与约束。

6. ER 到关系模式转换
   - 强实体集：转换为关系，简单属性成为关系属性；复合属性拆成简单属性。
   - 多值属性：单独建关系，包含实体主键和该多值属性。
   - 弱实体集：关系中包含拥有者主键和自身分辨符，二者组成主键。
   - 一对一联系：可把一方主键作为另一方外键，并加入联系属性。
   - 一对多联系：把“一”方主键加入“多”方关系。
   - 多对多联系：单独建联系关系，包含参与实体主键和联系属性。
   - 三元及以上联系：一般单独建关系，包含各参与实体主键和联系属性。

7. 扩展 ER 特性
   - 特化与泛化：超类/子类结构。
   - 约束：互斥/重叠，全部/部分。
   - 聚集：把联系抽象为高层实体参与另一联系。
   - UML 类图与 ER 图在表达实体、属性、关联和基数上有相似性。

8. 设计问题
   - 实体还是属性：看对象是否有独立属性、是否参与联系。
   - 实体还是联系：看概念是否表示对象本身还是对象间关联。
   - 二元还是非二元联系：不能随意把三元联系拆成多个二元联系，否则可能丢失语义。
   - 消除冲突：命名冲突、结构冲突、属性冲突、约束冲突。

## 考点整理

- 复习课件提示：实体集、弱实体集、属性集、联系集、聚集、描述性属性、映射基数、参与约束、主键、ER 转关系模式都是重点。
- 会根据文字需求画 ER 图，不能漏实体、属性、联系和约束。
- 会判断弱实体集，并写出其主键构成。
- 会把 1:1、1:n、m:n 联系转换为关系模式。
- 会处理复杂属性、多值属性和联系属性。
- 会说明聚集、特化、泛化的含义。

## 常见题型与答题抓手

- 画 ER 图：先列实体，再列属性，然后找联系，最后标基数、参与和主键。
- ER 转关系：按强实体、弱实体、多值属性、联系集逐项转换。
- 判断实体/属性/联系：看是否独立存在、是否有自身属性、是否参与多个联系。
- 三元联系转换：通常建独立关系，不要简单拆成三个二元联系。
""",
    "07_第7章_关系数据库设计与规范化.md": """# 第7章 关系数据库设计与规范化

## 章节定位

本章是理论和计算题重点，围绕函数依赖、属性闭包、候选键、正则覆盖、无损连接、依赖保持、BCNF、3NF、多值依赖和分解算法展开。

## 核心知识点

1. 好的关系模式设计
   - 目标：减少冗余，避免插入异常、删除异常、更新异常。
   - 坏设计常由不恰当合并模式造成。
   - 分解需要兼顾无损连接和依赖保持。

2. 第一范式 1NF
   - 属性域必须是原子的。
   - 非原子域包括集合、复合值、可再拆分编码等。

3. 函数依赖
   - `α -> β` 表示任意两个元组若在 `α` 上相同，则在 `β` 上也相同。
   - 平凡函数依赖：`β ⊆ α`。
   - 函数依赖用于刻画业务规则和判断模式质量。

4. 函数依赖闭包
   - `F+` 是由 `F` 能推出的所有函数依赖。
   - Armstrong 公理：自反律、增广律、传递律。
   - 常用规则：合并、分解、伪传递。

5. 属性闭包
   - `α+` 是在 `F` 下由属性集 `α` 能推出的全部属性。
   - 用途：
     - 判断 `α` 是否为超键。
     - 判断函数依赖 `α -> β` 是否成立。
     - 求候选键。
     - 辅助 BCNF/3NF 判定。

6. 候选键计算
   - 必须出现在键中的属性：不能由其他属性推出且必须覆盖全关系的属性。
   - 通过属性闭包判断是否能推出全部属性。
   - 候选键必须最小，去掉任一属性后不再是超键。

7. 正则覆盖
   - 目标：得到与原依赖集等价、右部单属性、无多余属性、无多余依赖的依赖集。
   - 多余属性：删除后依赖集闭包不变。
   - 多余依赖：删除后依赖集闭包不变。
   - 3NF 分解常基于正则覆盖。

8. 无损连接分解
   - 分解后自然连接能恢复原关系，不产生伪元组。
   - 二元分解 `R1, R2` 对 `R` 无损的常用判定：
     - `(R1 ∩ R2) -> R1` 或 `(R1 ∩ R2) -> R2` 属于 `F+`。

9. 依赖保持
   - 分解后不用连接各子关系就能检查原函数依赖。
   - BCNF 分解可能不保持依赖。
   - 3NF 分解可做到无损连接且保持依赖。

10. BCNF
   - 对每个非平凡函数依赖 `α -> β`，`α` 必须是超键。
   - 优点：冗余更少。
   - 缺点：可能不保持依赖。
   - BCNF 分解：找到违反 BCNF 的依赖 `α -> β`，分解为 `α ∪ β` 和 `R - (β - α)`，递归处理。

11. 3NF
   - 对每个非平凡函数依赖 `α -> β`，至少满足：
     - `α` 是超键；或
     - `β - α` 中每个属性都是某个候选键的主属性。
   - 3NF 允许一定冗余以保持依赖。
   - 3NF 分解：求正则覆盖，为每个依赖建关系，必要时加入包含候选键的关系，删除被包含的冗余关系。

12. 多值依赖
   - `α ->-> β` 表示在给定 `α` 时，`β` 与其余属性独立组合。
   - 多值依赖用于进一步规范化到 4NF。
   - 本课程复习提示要求知道多值依赖概念。

13. 去规范化
   - 出于性能考虑，有时故意保留冗余。
   - 需要权衡查询效率、更新代价和一致性维护成本。

## 考点整理

- 复习课件明确提示：函数依赖、属性闭包、正则覆盖、多值依赖概念、BCNF/3NF 判定与分解、Armstrong 公理和规则、无损连接和保持依赖证明都是重点。
- 会计算属性闭包和候选键。
- 会判断 BCNF 和 3NF。
- 会执行 BCNF 分解和 3NF 分解。
- 会证明或判断分解是否无损连接、是否保持依赖。
- 会使用 Armstrong 公理推导函数依赖。

## 常见题型与答题抓手

- 求属性闭包：从给定属性开始，反复应用左部已包含的依赖，直到不再增加属性。
- 求候选键：先找必须包含属性，再试探最小属性组合。
- 判 BCNF：检查每个非平凡依赖左部是否为超键。
- 判 3NF：若左部不是超键，看右部属性是否为主属性。
- BCNF 分解：每次用一个违反依赖分解，最后检查每个子模式。
- 3NF 分解：先求正则覆盖，再按依赖建表，补候选键，删包含表。
- 无损连接：二元分解用公共属性能否推出某一子模式。
- 依赖保持：把依赖投影到各子模式，闭包能否推出原依赖。
""",
    "17_第17章_事务.md": """# 第17章 事务

## 章节定位

本章是事务处理基础，重点是事务概念、ACID、事务状态、调度、冲突可串行化、视图可串行化、可恢复调度、无级联调度和 SQL 隔离级别。

## 核心知识点

1. 事务概念
   - 事务是访问并可能更新数据库的程序执行单元。
   - 典型例子：账户转账，必须保证扣款和加款作为整体完成。

2. ACID 特性
   - Atomicity 原子性：事务操作要么全做，要么全不做。
   - Consistency 一致性：事务单独执行应保持数据库一致性。
   - Isolation 隔离性：并发事务之间不应看到彼此的中间状态。
   - Durability 持久性：事务提交后结果应永久保存，即使系统故障也不丢失。

3. 事务状态
   - Active：事务正在执行。
   - Partially committed：最后一条语句已执行，等待提交。
   - Failed：事务无法继续正常执行。
   - Aborted：事务回滚，数据库恢复到事务开始前状态。
   - Committed：事务成功完成，结果永久写入。

4. 调度
   - 调度表示多个事务中读写操作的执行顺序。
   - 串行调度：事务一个接一个执行。
   - 并发调度：不同事务操作交错执行。
   - 正确并发执行的目标是效果等价于某个串行调度。

5. 冲突与冲突可串行化
   - 两个操作冲突需满足：属于不同事务、访问同一数据项、至少一个是写操作。
   - 冲突类型：`read-write`、`write-read`、`write-write`。
   - 通过交换不冲突操作可变成串行调度，则为冲突可串行化。

6. 优先图
   - 顶点表示事务。
   - 若事务 Ti 的操作与 Tj 的操作冲突且 Ti 先于 Tj，则加边 Ti -> Tj。
   - 调度冲突可串行化当且仅当优先图无环。
   - 若无环，拓扑序给出等价串行顺序。

7. 可恢复性
   - 可恢复调度：若 Tj 读了 Ti 写的数据，则 Tj 必须在 Ti 提交后才能提交。
   - 级联回滚：一个事务回滚导致读过其未提交数据的事务也必须回滚。
   - 无级联调度：事务只能读取已提交数据，避免级联回滚。

8. 隔离性实现与隔离级别
   - 并发异常：丢失修改、脏读、不可重复读、幻读。
   - SQL 隔离级别：Read Uncommitted、Read Committed、Repeatable Read、Serializable。
   - 隔离级别越高，并发异常越少，但并发性能可能越低。

9. SQL 中事务定义
   - `commit` 提交。
   - `rollback` 回滚。
   - 可设置事务隔离级别。

10. 视图可串行化
   - 比冲突可串行化更宽松。
   - 关注读从关系、最终写关系是否与某个串行调度相同。
   - 判定更复杂，课程重点通常是理解概念和能处理简单例子。

## 考点整理

- 复习课件提示：事务概念、模型、ACID、冲突/视图可串行化判别、事务隔离性和原子性、可恢复调度、无级联调度都是重点。
- 会画优先图并判断冲突可串行化。
- 会找冲突操作。
- 会判断调度是否可恢复、是否无级联。
- 会说明 SQL 隔离级别能避免哪些异常。
- 会解释 ACID 各性质在转账例子中的体现。

## 常见题型与答题抓手

- 判断冲突可串行化：列冲突，画优先图，看有无环。
- 判断可恢复：看读脏数据的事务是否在被读事务提交后再提交。
- 判断无级联：看是否读取未提交数据。
- 隔离异常题：先识别读写顺序，再判断是脏读、不可重复读、幻读还是丢失修改。
""",
    "18_第18章_并发控制.md": """# 第18章 并发控制

## 章节定位

本章讨论如何实现事务隔离性。重点是锁协议、两阶段锁、严格两阶段锁、强两阶段锁、死锁检测与恢复，以及多粒度锁、时间戳、验证、多版本和快照隔离等扩展机制。

## 核心知识点

1. 并发控制概览
   - 并发控制解决多个事务交错执行时的数据一致性问题。
   - 悲观并发控制：事前控制，典型是锁。
   - 乐观并发控制：先执行，提交前验证冲突。
   - 多版本机制：保存数据项多个版本，提高读写并发。

2. 锁模式
   - 共享锁 S：用于读，多个事务可同时持有。
   - 排他锁 X：用于写，与其他 S/X 锁通常不兼容。
   - 锁相容矩阵：S 与 S 相容，X 与 S/X 不相容。

3. 普通锁的问题
   - 只在读写前后随意加锁解锁不能保证可串行化。
   - 需要协议约束加锁和解锁顺序。

4. 两阶段锁协议 2PL
   - 增长阶段：只申请锁，不释放锁。
   - 收缩阶段：只释放锁，不再申请新锁。
   - 2PL 保证冲突可串行化。
   - 可能产生死锁。

5. 锁转换
   - 升级：S 锁升级为 X 锁。
   - 降级：X 锁降级为 S 锁。
   - 升级应在增长阶段，降级应在收缩阶段。

6. 严格两阶段锁
   - 事务持有所有 X 锁直到提交或回滚。
   - 可避免级联回滚，保证严格调度。

7. 强两阶段锁
   - 事务持有所有锁直到提交或回滚。
   - 实现更简单，隔离性更强，但并发度更低。

8. 自动加锁
   - 数据库系统可根据读写操作自动申请 S/X 锁。
   - 锁管理器维护锁表、等待队列和事务持锁信息。

9. 死锁
   - 多个事务互相等待对方释放资源，形成等待环。
   - 死锁预防：通过时间戳策略等避免形成死锁。
   - Wait-die：老事务可等新事务，新事务请求老事务持有的锁时回滚。
   - Wound-wait：老事务抢占新事务，新事务等待老事务。
   - 死锁检测：构造等待图，出现环则存在死锁。
   - 死锁恢复：选择牺牲事务回滚，释放锁。
   - 超时重启：等待过久则认为可能死锁并回滚。

10. 多粒度锁
   - 数据项可按数据库、表、页、记录等层次加锁。
   - 意向锁用于在高层表示低层将加锁。
   - 常见意向锁：IS、IX、SIX。
   - 加锁通常自顶向下，解锁自底向上。

11. 时间戳协议
   - 每个事务分配唯一时间戳。
   - 系统按时间戳顺序决定读写是否合法。
   - Thomas 写规则可忽略过时写，提高并发度。

12. 验证协议
   - 适合冲突较少场景。
   - 阶段：读阶段、验证阶段、写阶段。
   - 提交前验证是否与其他事务冲突。

13. 多版本机制与快照隔离
   - 多版本时间戳排序：读操作读取合适版本。
   - 多版本两阶段锁：读写使用不同版本减少冲突。
   - MVCC 支持快照读。
   - 快照隔离使用提交时检查，常见策略是 First Committer Wins。
   - 快照隔离不能完全等价于可串行化，可能有写偏斜等异常。

14. 插入、删除与索引并发
   - 插入和删除可能引入幻读。
   - 索引结构并发控制需要保护搜索路径和结构调整。

## 考点整理

- 复习课件明确提示：两阶段锁、严格两阶段锁、强两阶段锁、死锁检测与恢复是重点。
- 会判断 S/X 锁相容性。
- 会判断一个锁调度是否满足 2PL。
- 会说明 2PL 为什么保证冲突可串行化，以及为什么仍可能死锁。
- 会区分 2PL、严格 2PL、强 2PL。
- 会根据等待图判断死锁，并说明恢复方法。
- 了解多粒度锁和意向锁的作用。
- 了解时间戳、验证、多版本、快照隔离的基本思想。

## 常见题型与答题抓手

- 判断 2PL：找每个事务第一次释放锁后是否还申请新锁。
- 判断严格 2PL：看 X 锁是否持有到提交/回滚。
- 判断强 2PL：看所有锁是否持有到提交/回滚。
- 死锁检测：画等待图，有环则死锁。
- 锁相容题：S-S 可共存，X 与任何其他同项锁不兼容。
- 多粒度锁题：先申请意向锁，再申请低层 S/X 锁。
""",
    "所有章节总结.md": """# 数据库系统原理 所有章节总结

## 课程知识结构

本课程可分为七组核心内容：

1. 数据库系统概论：基本概念、DBMS 功能、数据抽象、模式/实例、数据模型、数据库语言、系统组件与体系结构。
2. 关系模型与关系代数：关系、键、完整性、选择、投影、并、差、笛卡尔积、重命名、连接、除法、聚集、外连接。
3. SQL：DDL、DML、DCL，基本查询、集合运算、空值、聚集、分组、嵌套查询、视图、完整性约束、授权、函数、过程、触发器。
4. ER 模型：实体、属性、联系、弱实体、映射基数、参与约束、主键、聚集、特化/泛化、ER 图设计与关系模式转换。
5. 关系数据库设计：函数依赖、属性闭包、候选键、正则覆盖、Armstrong 公理、无损连接、依赖保持、BCNF、3NF、多值依赖。
6. 事务：ACID、事务状态、调度、冲突可串行化、视图可串行化、可恢复调度、无级联调度、隔离级别。
7. 并发控制：锁、两阶段锁、严格两阶段锁、强两阶段锁、死锁、多粒度锁、时间戳、验证、多版本、快照隔离。

## 按章节速览

### 第1章 数据库系统概论

- 掌握 DB、DBMS、数据库系统、模式、实例、数据独立性。
- 能比较文件系统和数据库系统。
- 理解三级抽象：物理层、逻辑层、视图层。
- 理解 DDL、DML、SQL、存储管理、查询处理、事务管理。
- 考点：基本概念和简答题。

### 第2章 关系模型与关系代数

- 掌握关系、属性、元组、域、关系模式、关系实例。
- 掌握超键、候选键、主键、外键。
- 熟练写关系代数表达式：`σ`、`π`、`∪`、`-`、`×`、`ρ`、`⋈`、`÷`。
- 掌握聚集、外连接、数据库修改。
- 考点：根据自然语言写关系代数，特别是“所有/全部”对应除法或双重否定。

### 第3章 SQL 基础

- 会写 `create table`、主键、外键、非空约束。
- 熟练掌握 `select-from-where`。
- 掌握 `distinct`、`order by`、字符串匹配、集合运算。
- 掌握 `null` 与三值逻辑。
- 掌握聚集函数、`group by`、`having`。
- 掌握 `in`、`some`、`all`、`exists`、`not exists`、`with`。
- 考点：SQL 查询题、嵌套查询、聚集分组、`not exists`。

### 第4章 SQL 中级

- 掌握内连接、自然连接、左/右/全外连接。
- 掌握视图定义、视图展开、物化视图、可更新视图判断。
- 掌握完整性约束：`unique`、`check`、外键、级联。
- 掌握授权：`grant`、`revoke`、角色。
- 考点：连接查询、视图是否可更新、完整性约束和授权语句。

### 第5章 SQL 高级

- 理解 JDBC、ODBC、嵌入式 SQL 的作用。
- 理解游标处理多行查询结果。
- 掌握函数、存储过程、触发器的基本用途。
- 了解递归查询和固定点计算。
- 考点：函数/过程/触发器概念与简单应用。

### 第6章 ER 模型

- 会识别实体集、属性集、联系集、描述性属性。
- 会标注映射基数和参与约束。
- 会处理弱实体集、递归联系、聚集、特化/泛化。
- 会把 ER 图转换为关系模式。
- 考点：画完整 ER 图、ER 转关系模式，尤其强实体、弱实体、1:1、1:n、m:n、多值属性。

### 第7章 关系数据库设计与规范化

- 掌握函数依赖、属性闭包、候选键。
- 掌握 Armstrong 公理和常用推理规则。
- 掌握正则覆盖计算思想。
- 会判断无损连接和依赖保持。
- 会判定并分解到 BCNF、3NF。
- 了解多值依赖。
- 考点：闭包、候选键、BCNF/3NF 判定与分解、无损连接和依赖保持证明。

### 第17章 事务

- 掌握事务概念和 ACID。
- 掌握事务状态转换。
- 掌握调度、冲突、冲突可串行化、优先图。
- 掌握可恢复调度、无级联调度。
- 掌握隔离级别及并发异常。
- 考点：画优先图判冲突可串行化，判断可恢复/无级联，解释隔离异常。

### 第18章 并发控制

- 掌握 S 锁、X 锁和相容矩阵。
- 掌握 2PL、严格 2PL、强 2PL。
- 掌握死锁检测与恢复。
- 了解多粒度锁、意向锁、时间戳协议、验证协议、多版本和快照隔离。
- 考点：判断锁协议、判断死锁、区分 2PL/严格 2PL/强 2PL。

## 复习课件给出的考试范围

- 第 1 章：基本概念。
- 第 2 章：关系代数表达式，基本运算、附加运算、扩展运算。
- 第 3、4、5 章：SQL 语句，DDL、DML、DCL，集合运算、空值、聚集函数、`having`、`not exists`、嵌套子查询、视图、可更新视图、完整性约束、函数、过程、触发器。
- 第 6 章：ER 模型设计，关系模型转换，完整 ER 图；实体集、弱实体集、属性集、联系集、聚集、描述性属性、映射基数、参与约束、主键。
- 第 7 章：规范化，函数依赖、属性闭包、正则覆盖、多值依赖概念、BCNF/3NF 判定与分解、Armstrong 公理、无损连接和保持依赖证明。
- 第 17 章：事务概念、模型、ACID、冲突/视图可串行化、隔离性和原子性、可恢复调度、无级联调度。
- 第 18 章：两阶段锁、严格两阶段锁、强两阶段锁、死锁检测与恢复。

## 高优先级复习清单

1. 能独立写关系代数表达式，特别是除法和“全部”问题。
2. 能独立写 SQL 查询，特别是嵌套查询、`not exists`、分组聚集和 `having`。
3. 能画 ER 图并转换为关系模式。
4. 能求属性闭包、候选键、正则覆盖。
5. 能判断 BCNF/3NF 并完成分解。
6. 能证明无损连接和依赖保持。
7. 能画优先图判断冲突可串行化。
8. 能判断可恢复调度和无级联调度。
9. 能判断锁调度是否满足 2PL、严格 2PL、强 2PL。
10. 能画等待图判断死锁并说明恢复策略。

## 答题策略

- 概念题：写“定义 + 作用 + 例子/对比”。
- 关系代数题：先找涉及关系，再找连接条件，最后处理投影、差、除法。
- SQL 题：先写核心 `from` 和连接，再写 `where`，最后补聚集、分组、排序。
- ER 题：先实体，后属性，再联系，最后标基数、参与、主键。
- 规范化题：先求闭包和候选键，再判范式，最后按算法分解。
- 事务题：列冲突、画图、看环。
- 锁题：逐事务检查加锁/解锁阶段，死锁题画等待图。
""",
}


def main() -> None:
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    for name, content in SUMMARIES.items():
        (OUT_DIR / name).write_text(content.strip() + "\n", encoding="utf-8")
    print(f"wrote {len(SUMMARIES)} files to {OUT_DIR}")


if __name__ == "__main__":
    main()
